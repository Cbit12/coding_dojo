// CHANGE TEXT OF LOGIN BUTTON TO LOGOUT WHEN CLICKED
function logout(element) {
    element.innerText = "Logout";
}

// REMOVE BUTTON OF DEFINITION WHEN CLICKED
function hide(element) {
    element.remove();
}

// ADD ALERT IN LIKE BUTTON WHEN CLICKED => directo en el HTML